# Core numerics / strategy / backtest
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Data + dashboard
yfinance>=0.2.54
streamlit>=1.30.0
plotly>=5.18.0
python-dotenv

# Screener (S&P 500 scrape) - these were imported but never declared
requests
beautifulsoup4

# Scheduler (scheduler.py) - also previously undeclared
apscheduler
pytz

# AI features
langchain
langchain-community
# langchain-google-genai
langchain-azure-ai
duckduckgo-search
pydantic
ddgs

# Interactive Brokers (notebooks/ib_tws_connect.ipynb)
# Maintained successor to ib_insync. Needs a running TWS/Gateway; nothing else imports it.
ib_async

# Notebook execution
ipykernel

# Tests
pytest>=7.0
